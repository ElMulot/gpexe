type: 
    default: 'progress'

frequency:
    regex: '\d+'
    default: 7

exclude:
    -
        regex: '.+'
   
rules:
    -
        conditions:
            -
                regex: '.+'
        count:
            regex: '\d+'
        value:
            regex: '\d+'