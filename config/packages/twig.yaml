twig:
    default_path: '%kernel.project_dir%/templates'
    form_themes:
        - 'form/layout.html.twig'
        - 'form/dropzone.html.twig'
    globals:
        name: '%name%'
        webmaster: '%webmaster%'
        version: '%version%'
        date: '%date%'
        uploads_directory: '%uploads_directory%'
        icons_directory: '%icons_directory%'

when@test:
    twig:
        strict_variables: true
