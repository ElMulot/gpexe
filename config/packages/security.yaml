security:
    encoders:
        App\Entity\User:
            algorithm: auto


    # https://symfony.com/doc/current/security.html#where-do-users-come-from-user-providers
    providers:
        # used to reload user from session & other features (e.g. switch_user)
        app_user_provider:
            entity:
                class: App\Entity\User
                property: email
        # used to reload user from session & other features (e.g. switch_user)
    firewalls:
        dev:
            pattern: ^/(_(profiler|wdt)|css|images|js)/
            security: false
        main:
            anonymous: lazy
            provider: app_user_provider
            guard:
                authenticators:
                    - App\Security\LoginFormAuthenticator
            logout:
                path: logout
                success_handler: App\Security\LogoutSuccessHandler
        
            remember_me:
                secret:   '%kernel.secret%'
                lifetime: 604800 # 1 week in seconds
                path:     home
            
            # activate different ways to authenticate
            # https://symfony.com/doc/current/security.html#firewalls-authentication

            # https://symfony.com/doc/current/security/impersonating_user.html
            # switch_user: true

    # Easy way to control access for large sections of your site
    # Note: Only the *first* access control that matches will be used
    access_control:
        - { path: ^/login$,   roles: IS_AUTHENTICATED_ANONYMOUSLY }
        - { path: ^/logout$,  roles: IS_AUTHENTICATED_ANONYMOUSLY }
        - { path: ^/profil,   roles: ROLE_ADMIN }
        - { path: ^/company,  roles: ROLE_ADMIN }
        - { path: ^/user,     roles: ROLE_ADMIN }
        - { path: ^/admin,    roles: ROLE_ADMIN }
        - { path: .,          roles: ROLE_USER }
        
    role_hierarchy:
        ROLE_ADMIN: [ROLE_USER, ROLE_PROGRESS_RULES, ROLE_CODIFICATIONS, ROLE_METADATAS, ROLE_VISAS, ROLE_EDIT, ROLE_MOVE]
        ROLE_MOVE: [ROLE_EDIT]
