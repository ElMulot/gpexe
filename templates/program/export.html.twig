<div class="row justify-content-md-center">
	<button type="button" class="btn btn-primary w-25 m-1" data-toggle="ajax" data-url="{{ path('program_preload', {'program': program.id}) }}" data-target="#output">Nouveau export</button>
</div>

<div class="card mt-5">
    <div class="card-body">
    	<h5 class="card-title">Operation terminée !</h5>
        <p class="card-text">Le fichier est disponible au lien ci-dessous</p>
        <p class="text-center">
        	<a href="{{ asset(file_path) }}" class="btn btn-primary">Lien de téléchargement</a>
        </p>
    </div>
</div>