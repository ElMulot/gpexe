<turbo-frame id="view_pannel">
	<div class="row px-2" {{ stimulus_controller('engineering--view', {'id': selected_view?selected_view.id}) }}>
		<div class="col px-2 nav-items">
			{% for view in views %}
				<div class="btn-group" role="group">
					<button type="button" class="btn" 
						{{ stimulus_target('engineering--view', 'button') }} 
						{{ stimulus_action('engineering--view', 'update') }} 
						{{ stimulus_param('engineering--view', {'id': view.id}) }}>
							{{ view.name }}
					</button>
					{% if is_granted('VIEW_EDIT', view) or is_granted('VIEW_DELETE', view) %}
						<div class="btn-group dropend" role="group">
							<button type="button" class="btn dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false" 
								{{ stimulus_target('engineering--view', 'button') }} {{ stimulus_param('engineering--view', {'id': view.id}) }}>
							</button>
							<ul class="dropdown-menu">
								{% if is_granted('VIEW_EDIT', view) %}
									<li>
										<a class="dropdown-item text-success" href="{{ path('viewEdit', {'view': view.id}) }}" data-turbo-frame="xl_modal" data-turbo-stream-replace="view_pannel">
											{% trans %}Edit{% endtrans %}
										</a>
									</li>
								{% endif %}
								{% if is_granted('VIEW_DELETE', view) %}
									<li>
										<a class="dropdown-item text-danger" href="{{ path('viewDelete', {'view': view.id}) }}" data-turbo-frame="xl_modal" data-turbo-stream-replace="view_pannel">
											{% trans %}Delete{% endtrans %}
										</a>
									</li>
								{% endif %}
							</ul>
						</div>
					{% endif %}
				</div>
			{% endfor %}
		</div>
		<div class="col-auto">
			<a class="btn btn-primary btn-width" href="" data-turbo-frame="md_modal">
				{% trans %}Export view{% endtrans %}
			</a>
		</div>
		{% if is_granted('VIEW_NEW', project) %}
			<div class="col-auto">
				<a class="btn btn-primary btn-width" href="{{ path('viewNew', {'project': project.id }) }}" data-turbo-frame="xl_modal" data-turbo-stream-replace="view_pannel">
					{% trans %}new.view{% endtrans %}
				</a>
			</div>
		{% endif %}
	</div>
</turbo-frame>