{% if app.request.headers.get('Turbo-frame') == 'edit_pannel' %}

	<turbo-frame id="edit_pannel">
		{{ form(form, {'attr': {'id': 'form', 'action': app.request.uri, 'data-turbo-stream-replace': 'tbody' }}) }}
	</turbo-frame>

{% else %}

	{% include('generic/edit.html.twig') %}

{% endif %}