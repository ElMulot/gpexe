{% if app.request.headers.get('Turbo-frame') == 'new_pannel' %}

	<turbo-frame id="new_pannel">
		{{ form(form, {'attr': {'data-turbo-stream-replace': 'tbody' }}) }}
	</turbo-frame>

{% else %}

	{% include('generic/new.html.twig') %}

{% endif %}