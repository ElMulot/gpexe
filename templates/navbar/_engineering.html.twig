{% import "macros/_navbar.html.twig" as navbar %}

<ul class="navbar-nav me-auto">
	{% set serie_ids = series|map(v => v.id) %}
	
	{{ navbar.item('Back', path('project', {'project': project.id})) }}

	{{ navbar.dropdown(
		{
			'name': 'Action',
			'id': 'action',
			'style': 'display:none',
		}, [
			{
				'name': 'New',
				'href': path('engineering_new', {'project': project.id, 'series': serie_ids}),
				'data-turbo-frame': 'xl_modal',
			},
			{
				'name': 'Edit',
				'href': path('engineering_edit', {'project': project.id}),
				'data-turbo-frame': 'xl_modal',
			},
			{
				'name': 'Move',
				'href': path('document_move', {'project': project.id}),
				'data-turbo-frame': 'xl_modal',
			},
			{
				'name': 'Delete',
				'href': path('engineering_delete', {'project': project.id}),
				'data-turbo-frame': 'xl_modal',
			},
		])
	}}
</ul>

{{ navbar.title("Documents")}}

<ul class="navbar-nav ms-auto">
	{{ navbar.itemLogout() }}
</ul>
