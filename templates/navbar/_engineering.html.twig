{% import "macros/_navbar.html.twig" as navbar %}

<ul class="navbar-nav me-auto">
	{% set serie_ids = series|map(v => v.id) %}
	
	{{ navbar.item('Back', path('project', {'project': project.id})) }}

	{{ navbar.dropdown(
		{
			'name': 'Action',
			'id': 'action',
			'style': 'display:none',
		}, [
			{
				'name': 'New',
				'href': path('engineeringNew', {'project': project.id, 'series': serie_ids}),
				'data-turbo-frame': 'xl_modal',
				'data-turbo-stream-replace': 'serie_pannel',
			},
			{
				'name': 'Edit',
				'href': path('engineeringEdit', {'project': project.id}),
				'data-turbo-frame': 'xl_modal',
				'data-turbo-stream-replace': 'serie_pannel',
			},
			{
				'name': 'Move',
				'href': path('documentMove', {'project': project.id}),
				'data-turbo-frame': 'xl_modal',
				'data-turbo-stream-replace': 'serie_pannel',
			},
			{
				'name': 'Delete',
				'href': path('engineeringDelete', {'project': project.id}),
				'data-turbo-frame': 'xl_modal',
				'data-turbo-stream-replace': 'serie_pannel',
			},
		])
	}}
</ul>

{{ navbar.title("Documents")}}

<ul class="navbar-nav ms-auto">
	{{ navbar.itemLogout() }}
</ul>
