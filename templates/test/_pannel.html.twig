<turbo-frame id="pannel">

	<p>Turbo-Frame: <span class='border p-1'>{{ app.request.headers.get('Turbo-Frame') ?? 'n/a' }}</span></p><br/>
	<p>Turbo-Stream-Replace: <span class='border p-1'>{{ app.request.headers.get('Turbo-Stream-Replace') ?? 'n/a' }}</span></p><br/>
	<p>Text: <span class='border p-1'>{{ text ?? 'n/a' }}</span></p>

</turbo-frame>
