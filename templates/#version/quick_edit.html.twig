{% if form is defined %}
	
	{{ form_start(form, { 'attr': {'data-toggle': 'ajax', 'data-url': app.request.uri}}) }}
	{% for child in form %}
		{{ form_widget(child) }}
	{% endfor %}
	{{ form_end(form) }}

{% else %}
	
	{{ property|to_string }}
	
{% endif %}